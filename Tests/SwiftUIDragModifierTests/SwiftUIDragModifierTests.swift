import XCTest
@testable import SwiftUIDragModifier

final class SwiftUIDragModifierTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUIDragModifier().text, "Hello, World!")
    }
}
